export function EmptyState() {
  return (
    <section className="section-shell mt-8 pb-12">
      <div className="glass-card p-8 text-center">
        <p className="soft-label">Ready For Matching</p>
        <h3 className="mt-2 text-2xl font-semibold tracking-[-0.03em] text-slate-950">Start with a student profile</h3>
        <p className="mx-auto mt-3 max-w-2xl text-sm leading-7 text-slate-600">
          Once the profile is submitted, this workspace turns a broad summer search into a ranked set of options, profile impact signals, and a clear next-step plan.
        </p>
      </div>
    </section>
  );
}
